// Design-sync entry: the brand "design system" surface synced to claude.ai/design.
// Only the presentational, brand-bearing components — the Supabase/data-coupled
// ones (LikeButton, CommentsSection) are intentionally excluded.
export { Button } from "../src/components/Button";
export { Gallery } from "../src/components/Gallery";
export { SiteHeader } from "../src/components/SiteHeader";
export { SiteFooter } from "../src/components/SiteFooter";
